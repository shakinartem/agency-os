[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "agency-os-shared"
version = "0.1.0"
description = "Shared types, DTOs and enums for Agency OS"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7.0",
]
